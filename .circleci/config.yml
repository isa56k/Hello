version: 2.1

orbs:
  python: circleci/python:3.6.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: make install
          name: Install Dependencies

workflows:
  main:
    jobs:
      - build-and-test
